#include <bits/stdc++.h>

using namespace std;

int Min(int a, int b, int c) {
    return ::min(::min(a, b), c);
}

int main()
{
    int a1, b1, c1; cin >> a1 >> b1 >> c1;
    int a2, b2, c2; cin >> a2 >> b2 >> c2;
    int b3, c3; cin >> b3 >> c3;

    int s_1 = a1 + b1 + c1;
    int s_2 = a2 + b2 + c2;
    int s_3 = b3 + c3;
}